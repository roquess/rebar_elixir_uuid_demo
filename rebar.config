{erl_opts, [debug_info]}.

{plugins, [
    {rebar_mix, "0.4.0"}
]}.

{provider_hooks, [
    {pre, [{compile, {mix, find_elixir_libs}}]},
    {post, [{compile, {mix, consolidate_protocols}}]}
]}.

{deps, [
     {elixir_uuid, "1.2.1"}
]}.

{shell, [
    {apps, [rebar_elixir_uuid_demo]}
]}.
